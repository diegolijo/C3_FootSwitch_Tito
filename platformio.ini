; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32-c3-devkitm-1]
platform = espressif32
board = esp32-c3-devkitm-1
framework = arduino

monitor_speed = 115200
monitor_rts = 0                 ; Desactiva señal RTS (evita reset automático)
monitor_dtr = 0                 ; Desactiva señal DTR (evita reset automático)
monitor_filters = direct        ; Desactiva filtros, muestra datos directos

build_flags = 
    -D ARDUINO_USB_CDC_ON_BOOT=1    ; Habilita USB CDC al iniciar
    -D ARDUINO_USB_MODE=1           ; Modo USB (no UART tradicional)

platform_packages = framework-arduinoespressif32    ; Fuerza versión específica del framework


